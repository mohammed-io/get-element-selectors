{
  "name": "get-element-selectors",
  "version": "1.0.2",
  "description": "Get element CSS selectors",
  "main": "src/index.ts",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "jest --config jestconfig.json",
    "test-watch": "jest --watch --config jestconfig.json",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.{js,ts}\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint"
  },
  "files": [
    "lib/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/linsight/get-element-selectors.git"
  },
  "keywords": [
    "css selector",
    "reverse",
    "querySelector"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/linsight/get-element-selectors/issues"
  },
  "homepage": "https://github.com/linsight/get-element-selectors#readme",
  "devDependencies": {
    "@types/jest": "^26.0.8",
    "jest": "^26.2.2",
    "prettier": "^2.0.5",
    "ts-jest": "^26.1.4",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.9.7"
  }
}
